// Authors: Yousef Yousef
// Purpose: Natural Burial informational page for Woodland Conservation
import React, { useRef, useState } from "react";
import ShroudImage from "../assets/download-4.jpg";
import WillowCasket from "../assets/download-8.jpg";
import MemorialTree from "../assets/image2.jpg";
import MeadowTrail from "../assets/forest.jpg";
import { speakSoftly } from "../utils/speakSoftly";

const NaturalBurial = ({ dark }) => {
  const [isPaused, setIsPaused] = useState(false);
  const [selectedPlan, setSelectedPlan] = useState(null);
  const [selectedCasket, setSelectedCasket] = useState("Shroud");
  const [markerChoice, setMarkerChoice] = useState("Living marker (groundcover)");
  const [selectedBurialOption, setSelectedBurialOption] = useState("Shroud Burial");
  const [selectedFamilyPlot, setSelectedFamilyPlot] = useState("Paired plots");
  const [addons, setAddons] = useState({
    nativeFlowers: true,
    qrMemories: false,
    gpsTag: false,
  });
  const burialRef = useRef(null);
  const subscriptionRef = useRef(null);

  const burialOptions = [
    {
      title: "Shroud Burial",
      image: ShroudImage,
      alt: "Handwoven burial shroud resting on a wooden frame",
      description:
        "Soft, breathable plant fibers with no metal or plastic. Ideal for those wanting the lightest footprint.",
      notes: [
        "Family-led lowering support and farewell ritual",
        "Local wildflower seed scatter at the graveside",
        "Optional keepsake ribbon for loved ones",
      ],
    },
    {
      title: "Biodegradable Willow Casket",
      image: WillowCasket,
      alt: "Willow casket ready for natural burial",
      description:
        "Crafted from willow and natural cotton fittings. Breaks down gently while protecting the woodland floor.",
      notes: [
        "Earth-friendly linings and natural closures",
        "Understated marker that blends with the forest",
        "Space for letters, flowers, or personal mementos",
      ],
    },
    {
      title: "Memorial Tree & Groundcover",
      image: MemorialTree,
      alt: "Young memorial tree surrounded by woodland groundcover",
      description:
        "Honor a life with a native tree or low groundcover that strengthens soil health and supports pollinators.",
      notes: [
        "Species chosen with our ecologist to fit the terrain",
        "Seasonal stewardship to help roots establish",
        "Placement mapped so families can always return",
      ],
    },
  ];

  const subscriptionPlans = [
    {
      name: "Steward",
      price: "$18/mo",
      description: "Keep trails open, benches safe, and staff ready to assist every visit.",
      features: [
        "Priority visit scheduling and guided assistance",
        "Quarterly woodland health updates",
        "One native wildflower scatter each year",
      ],
      accent: "border-gray-300",
    },
    {
      name: "Grove",
      price: "$42/mo",
      description: "For families wanting hands-on care for a resting place and the surrounding habitat.",
      features: [
        "Seasonal groundcare at the burial site",
        "Annual soil check and invasive-species sweep",
        "Dedicated memorial tree or groundcover support",
      ],
      accent: "border-sky-400 shadow-lg shadow-sky-100/60 dark:shadow-sky-900/40",
      highlight: true,
    },
    {
      name: "Legacy",
      price: "$88/mo",
      description: "Long-view stewardship that funds canopy restoration and education for the community.",
      features: [
        "Two guided remembrance gatherings per year",
        "Canopy restoration contribution in your name",
        "Custom marker refresh or replacement as needed",
      ],
      accent: "border-amber-400",
    },
  ];

  const handleSpeechToggle = () => {
    const synth = window.speechSynthesis;
    if (!synth) return;

    if (synth.paused) {
      synth.resume();
      setIsPaused(false);
      return;
    }

    if (synth.speaking) {
      synth.pause();
      setIsPaused(true);
      return;
    }

    const text =
      "Natural burial at Woodland Conservation focuses on biodegradable shrouds, willow caskets, and living memorial trees that keep the forest healthy. Paths remain accessible with seating, and guides can accompany visitors who need support. Stewardship subscriptions like Steward, Grove, and Legacy fund trail care, soil health, and canopy restoration so each resting place continues to nurture the woodland.";

    speakSoftly(text);
    setIsPaused(false);
  };

  const toggleAddon = (key) => {
    setAddons((prev) => ({ ...prev, [key]: !prev[key] }));
  };

  const scrollToSection = (ref) => {
    if (ref?.current) {
      ref.current.scrollIntoView({ behavior: "smooth", block: "start" });
    }
  };

  return (
    <div
      className={`min-h-screen font-[Calibri,ui-sans-serif] ${
        dark ? "bg-gray-900 text-amber-100" : "bg-white text-gray-900"
      }`}
      style={{ cursor: "auto" }}
    >
      {/* Hero */}
      <section
        className={`w-full bg-gradient-to-r ${
          dark
            ? "from-[#0e1a2b] via-[#0f2438] to-[#0e1a2b]"
            : "from-[#e8edf5] via-[#f1f5fb] to-[#e8edf5]"
        } text-center py-16 px-6 shadow-inner`}
      >
        <div className="max-w-5xl mx-auto">
          <h1 className="font-serif text-4xl md:text-5xl font-bold tracking-tight">
            Natural Burial at Woodland Conservation
          </h1>
          <p className="mt-4 text-lg text-gray-700 dark:text-gray-200 max-w-3xl mx-auto">
            Forest-first burials with biodegradable choices, gentle groundcare, and subscriptions
            that keep every path and marker tended.
          </p>
          <div className="mt-8 flex flex-wrap justify-center gap-3">
            <button
              onClick={() => scrollToSection(burialRef)}
              className="px-5 py-3 rounded-full bg-white/90 text-sky-800 font-semibold shadow hover:shadow-lg hover:-translate-y-0.5 transition border border-sky-300"
            >
              Explore Burial Options
            </button>
            <button
              onClick={() => scrollToSection(subscriptionRef)}
              className="px-5 py-3 rounded-full bg-sky-700 text-white font-semibold shadow hover:shadow-lg hover:-translate-y-0.5 transition border border-sky-600"
            >
              Stewardship Plans
            </button>
          </div>
        </div>
      </section>

      {/* Content */}
      <section className="max-w-6xl mx-auto px-6 py-12 md:py-16">
        <div className="mb-8 flex items-center gap-3 flex-wrap">
          <button
            onClick={handleSpeechToggle}
            className="px-4 py-2 border rounded bg-gray-100 hover:bg-gray-200 dark:bg-gray-800 dark:hover:bg-gray-700 text-sm text-[#5b3a1a] dark:text-white"
            aria-label={isPaused ? "Resume text to speech" : "Play or pause text to speech"}
            title={isPaused ? "Resume text to speech" : "Play or pause text to speech"}
          >
            {isPaused ? "Resume speech" : "Play / Pause speech"}
          </button>
        </div>

        <div className="grid md:grid-cols-2 gap-10 md:gap-14 items-center">
          {/* Image */}
          <div className="flex justify-center">
            <img
              src={MemorialTree}
              alt="Memorial tree in woodland clearing"
              className="w-full max-w-xl rounded-2xl shadow-2xl object-cover transition duration-700 ease-out md:hover:scale-[1.01] md:hover:shadow-3xl"
            />
          </div>

          {/* Text */}
          <div className="space-y-5 text-lg leading-relaxed md:pr-2">
            <p>
              Natural burial keeps the woodland as the focus. Biodegradable
              shrouds and willow caskets rest directly in the earth with
              understated markers, so the forest remains vibrant while honoring
              each person with quiet dignity.
            </p>
            <p>
              Accessibility is woven into the landscape: paths are tended,
              seating rests along routes, and guides are available for anyone
              needing support. The calm setting invites reflection without
              sacrificing comfort for elders, children, or visitors with
              mobility needs.
            </p>
            <p>
              Stewardship continues well beyond the service. Native plantings,
              memorial trees, and living groundcovers strengthen biodiversity
              while subscriptions fund trail care, soil health, and canopy
              restoration for future generations.
            </p>
          </div>
        </div>

        {/* Definition */}
        <div className="mt-10 rounded-2xl border border-slate-200 dark:border-slate-800 bg-white/90 dark:bg-gray-800/80 shadow-lg p-6 md:p-8">
          <h2 className="text-2xl font-semibold mb-3">What is natural burial?</h2>
          <p className="text-base md:text-lg leading-relaxed text-gray-800 dark:text-amber-100">
            A simple return to the woodland without embalming, vaults, polished stone, or plastics. Bodies rest in
            biodegradable shrouds or caskets. Markers stay low and breathable—living plants, wood slices, or digital
            memory pebbles—so the forest remains the focus.
          </p>
        </div>

        {/* Burial essentials */}
        <div className="mt-14" ref={burialRef}>
          <div className="flex flex-col md:flex-row md:items-center md:justify-between gap-4 mb-6">
            <h2 className="font-serif text-3xl font-semibold tracking-tight">
              Natural Burial Essentials
            </h2>
            <p className="text-sm md:text-base text-gray-600 dark:text-gray-300 max-w-2xl">
              Choose the material and memorial approach that matches your values. Each option is
              fully biodegradable and guided by our woodland caretakers—no polished stone, no plastic,
              just natural fibers, living plants, and soil-safe fittings.
            </p>
          </div>
          <div className="grid md:grid-cols-3 gap-8">
            {burialOptions.map((option) => {
              const isSelected = selectedBurialOption === option.title;
              return (
              <div
                key={option.title}
                role="button"
                tabIndex={0}
                onClick={() => setSelectedBurialOption(option.title)}
                onKeyDown={(e) => {
                  if (e.key === "Enter" || e.key === " ") {
                    e.preventDefault();
                    setSelectedBurialOption(option.title);
                  }
                }}
                className={`group rounded-2xl overflow-hidden border bg-white/80 dark:bg-gray-800/70 shadow-md dark:shadow-xl dark:shadow-black/20 transition-transform duration-300 ease-out hover:-translate-y-1 hover:scale-[1.02] hover:shadow-2xl cursor-pointer ${
                  dark ? "border-gray-700 hover:border-sky-400/80" : "border-gray-200 hover:border-sky-500/70"
                } ${isSelected ? "ring-2 ring-sky-500" : ""}`}
              >
                <div className="h-48 overflow-hidden">
                  <img
                    src={option.image}
                    alt={option.alt}
                    className="w-full h-full object-cover transition duration-500 ease-out group-hover:scale-[1.05]"
                  />
                </div>
                <div className="p-5 space-y-3">
                  <div className="flex items-center justify-between">
                    <h3 className="font-semibold text-xl">{option.title}</h3>
                    <span className="text-xs uppercase tracking-wide text-sky-700 dark:text-sky-300">
                      Low impact
                    </span>
                  </div>
                  <p className="text-sm leading-relaxed text-gray-700 dark:text-gray-200">
                    {option.description}
                  </p>
                  <ul className="text-sm space-y-2 text-gray-700 dark:text-gray-300">
                    {option.notes.map((note) => (
                      <li key={note} className="flex items-start gap-2">
                        <span className="mt-1 h-2 w-2 rounded-full bg-sky-600 dark:bg-sky-400" />
                        <span>{note}</span>
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Customization */}
        <div className="mt-16 rounded-3xl border border-sky-200 dark:border-sky-900/50 bg-sky-50/60 dark:bg-gray-900/60 shadow-lg p-8 md:p-10">
          <div className="flex flex-col md:flex-row md:items-start md:justify-between gap-6">
            <div className="space-y-3 md:max-w-sm">
              <p className="text-sm font-semibold uppercase tracking-[0.2em] text-sky-700 dark:text-sky-300">
                Customize Naturally
              </p>
              <h3 className="font-serif text-2xl md:text-3xl font-semibold">
                Every detail stays biodegradable and forest-first
              </h3>
              <p className="text-gray-700 dark:text-gray-200 leading-relaxed">
                Select the casket style, marker approach, and gentle add-ons. Options are designed to
                honor the land: woven fibers, living markers, native flowers, and optional guidance
                tools like discreet GPS or QR memories if your family prefers digital wayfinding.
              </p>
            </div>

            <div className="grid sm:grid-cols-2 gap-6 flex-1">
              <div className="p-5 rounded-2xl bg-white/90 dark:bg-gray-800/80 border border-sky-200 dark:border-sky-800 shadow">
                <h4 className="font-semibold mb-3">Casket preference</h4>
                <div className="space-y-2 text-sm text-gray-800 dark:text-gray-200">
                  {["Shroud", "Willow Casket"].map((casket) => (
                    <label key={casket} className="flex items-center gap-2 cursor-pointer">
                      <input
                        type="radio"
                        name="casket"
                        value={casket}
                        checked={selectedCasket === casket}
                        onChange={() => setSelectedCasket(casket)}
                      />
                      <span>{casket}</span>
                    </label>
                  ))}
                </div>
              </div>

              <div className="p-5 rounded-2xl bg-white/90 dark:bg-gray-800/80 border border-sky-200 dark:border-sky-800 shadow">
                <h4 className="font-semibold mb-3">Marker style</h4>
                <select
                  value={markerChoice}
                  onChange={(e) => setMarkerChoice(e.target.value)}
                  className="w-full rounded-lg border border-gray-300 dark:border-gray-700 bg-white dark:bg-gray-900 px-3 py-2 text-sm"
                >
                  <option>Living marker (groundcover)</option>
                  <option>Memorial tree with natural tag</option>
                  <option>Flat wood slice marker</option>
                  <option>QR memory pebble (low-profile)</option>
                </select>
                <p className="mt-2 text-xs text-gray-600 dark:text-gray-400">
                  We avoid polished stones; markers stay low and breathable so roots and moss can thrive.
                </p>
              </div>

              <div className="p-5 rounded-2xl bg-white/90 dark:bg-gray-800/80 border border-sky-200 dark:border-sky-800 shadow">
                <h4 className="font-semibold mb-3">Add-ons</h4>
                <div className="space-y-2 text-sm text-gray-800 dark:text-gray-200">
                  <label className="flex items-center gap-2 cursor-pointer">
                    <input
                      type="checkbox"
                      checked={addons.nativeFlowers}
                      onChange={() => toggleAddon("nativeFlowers")}
                    />
                    <span>Seasonal native flowers at visits</span>
                  </label>
                  <label className="flex items-center gap-2 cursor-pointer">
                    <input
                      type="checkbox"
                      checked={addons.qrMemories}
